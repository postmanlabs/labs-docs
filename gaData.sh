#!/bin/bash



# find H2 tag in diff
result=process.env
echo ${#result};
printenv

# size=${#result}
#   if [ $size > 0 ]
#   then
#     # append data to email
#     printf '%s\n' "${result[@]}" > $email
#     # send mail to email address
#     node email.js
#   else
#     echo no data
#   fi
